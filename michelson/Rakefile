require 'hoe'
require './lib/michelson/version.rb'

Hoe.spec 'michelson' do

  self.version = Michelson::VERSION

  self.summary = "michelson - test simulator / runtime for type-safe 'n' functional (crypto) contracts"
  self.description = summary

  self.urls    = ['https://github.com/s6ruby/ruby-to-michelson']

  self.author  = 'Gerald Bauer'
  self.email   = 'wwwmake@googlegroups.com'

  # switch extension to .markdown for gihub formatting
  self.readme_file  = 'README.md'
  self.history_file = 'CHANGELOG.md'

  self.extra_deps = [
  ]

  self.licenses = ['Public Domain']

  self.spec_extras = {
    required_ruby_version: '>= 2.2.2'
  }

end
